spring:
  datasource:
    name: store
    url: jdbc:mysql://localhost:3306/store
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bs.store.entity
server:
  port: 80

